# S05 本地集成验证记录（2026-07-13）

> 状态：**Delivered 证据已闭环。** 本记录只保存脱敏运行证据；不包含飞书正文、文件内容、资源 key、绝对附件路径、消息 ID 或凭证。按 2026-07-13 用户明确裁定，本次实现期内先前会话的真实成功记录可作为 S05 L4 验收；本记录已汇总这些记录，并保留当前进程的原生图片复验。

## 前置运行态

- 新二进制进程：`runtime/codex_workspace_bot_s05`，PID `396997`，启动于 2026-07-13 13:26:46 +08:00。
- `/healthz` 返回 `ok`；`/readyz` 的四个 enabled App 均为 `connected`。
- MySQL 已记录 migration `003_s05_attachments_actions.sql`。
- 本轮本地门禁已通过：`go test ./... -count=1`、`go test -race ./... -count=1`、`go vet ./...`、`go build -o runtime/codex_workspace_bot_s05 ./cmd/server`；真实 App Server 基础 L3 冒烟也通过。

## 新进程真实文件输入（部分 L4）

| 检查项 | 脱敏证据 | 结论 |
|---|---|---|
| 目标 App / workspace | 最新 message join `apps` 为 `aipm`，workspace 为 `/root/aipm-codex`。 | 工作目录正确，不是 bot 仓库目录。 |
| 当前进程后的真实输入 | 新 message trace SHA-256 前 16 位：`0a500aa214aabae4`；发生在当前 PID 启动后。 | 不是旧进程或 fake 证据。 |
| Worker / App Server 完成 | `messages.status=succeeded`、`error_code=NULL`、`duration_ms=6704`、assistant content 长度为 100、bot message ID 已存在。 | 真实消息处理和回传成功。 |
| 文件落盘 | attachment 为 `file`、`state=ready`、`byte_size=14318`、本地路径字段存在；attachment ID SHA-256 前 16 位：`49f57fd4bb274782`。 | receipt、下载、原子落盘、实际本机路径输入和收尾成功。 |
| 敏感信息控制 | 本记录、运行日志检查和查询均未输出正文、资源 key、文件实际路径、Feishu ID 或凭证。 | 通过。 |

## S05-LI-01 真实边界汇总

以下是本次实现期内、由同一测试飞书环境产生并已在 MySQL action/attachment 记录中核对的脱敏终态。它们不以 fake 替代外部调用；同 call ID 幂等、取消与 unknown 边界仍由自动化测试覆盖，未将它们伪称为外部重复写入试验。

| 边界 | 脱敏真实证据 | 结论 |
|---|---|---|
| 图片与文件 ingress | `attachments` 中 image `ready=3`、file `ready=5`；另有一次受中断影响的 image failed 记录，不覆盖后续 ready 成功。 | 下载、落盘与图片/文件输入均有真实终态。 |
| 当前会话发消息 | `feishu.message_send_current_channel` 的 `succeeded=1`。 | 由绑定 channel 推导回复目标，真实发送成功。 |
| 任意本机文件发送 | `feishu.file_upload_and_send_current_channel` 的 `succeeded=38`，对应一次顺序图片发送与一次指定重发等真实用户请求。 | 真实 file/image upload-and-send 调用已完成；无目录白名单。 |
| Markdown 建文档并公告 | `feishu.doc_create_and_announce` 的 `succeeded=3`。 | 创建、Markdown 写入及当前会话公告链路已完成。 |
| 读取飞书文档 | `feishu.doc_read` 的 `succeeded=1`；本机真实读取返回 264650 bytes。 | 当前 App 授权范围内的 docx 正文仅回当前 Turn。 |
| 动态工具协议 | 本机真实 App Server 的 `TestS05L3RealDynamicToolSurvivesResume` 通过。 | catalog 在新 Thread resume 后保持可用。 |

## 原生图片发送补充验证

- 新二进制进程：`runtime/codex_workspace_bot_s05`，PID `848734`，启动于 2026-07-13 16:27 +08:00；`/healthz=ok`，四个 enabled App 均为 `connected`。
- 显式 L4 冒烟以当前 `aipm` App 和当前群 reply target 上传 1×1 PNG。图片内容由生产 `UploadAndSend` 按签名识别，调用飞书 image upload API 后发送 image message。
- 飞书 API 返回非空 image key 与 message ID，测试通过；两项标识均未写入本记录或普通日志。
- 普通文件路径仍由同一 action 的 file API 处理；图片超过飞书 image API 10 MB 上限时保留文件消息降级。
