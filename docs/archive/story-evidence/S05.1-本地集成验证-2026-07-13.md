# S05.1 本地集成验证（2026-07-13）

## 新进程

- 二进制：`runtime/codex_workspace_bot_s05_1`
- 当前启动时间：2026-07-13 20:45:40 +08:00（Owner transfer/URL announcement timeout 修复后的新进程）
- `/healthz`：`ok`
- `/readyz`：4 个已配置 App 均为 `connected`
- 历史 action ledger 基线：`feishu.doc_create_and_announce` 为 56 个 `succeeded`、1 个 `in_flight`；旧记录不含 Owner transfer 结果，不能当作 S05.1 证据。

## 真实飞书 L4

两个显式 opt-in 测试都只从 MySQL 选择最近的可信 ingress sender；不打印 app secret、open ID、chat ID、document ID 或 URL。

| 场景 | 命令 | 结果 |
|---|---|---|
| p2p | `S05_RUN_OWNER_TRANSFER_L4=1 go test ./internal/feishu -run '^TestS051L4DocumentOwnerTransfer$' -count=1 -v` | 2026-07-13 20:40:01 +08:00 首次通过；timeout 修复后的新进程于 20:45:54 +08:00 再次通过。新 docx 内容写入、原 p2p 公告均成功，Drive 返回 `owner_transferred=true` / `transferred`。 |
| group | `S05_RUN_OWNER_TRANSFER_L4=1 go test ./internal/feishu -run '^TestS051L4GroupDocumentOwnerTransfer$' -count=1 -v` | 2026-07-13 20:40:42 +08:00 首次通过；timeout 修复后的新进程于 20:45:59 +08:00 再次通过。URL 发送到 group `chat_id`，但 Drive Owner 使用该 group 最近入站消息的 `sender_open_id`，返回 `owner_transferred=true` / `transferred`。 |

这两个测试各创建一篇带唯一标题的测试文档并公告到相应原会话；Owner 转移的成功响应是本次外部服务的权威结果。测试没有调用模型工具参数来指定 Owner，也没有使用群 chat ID 作为 Owner。

## 残余门禁

`go test ./...`、`go vet ./...`、相关 `go test -race` 与服务 build 均通过。全仓 `go test -race ./...` 当前编译失败于 S05.1 无关、并行中的 `internal/worker/manager_internal_test.go`：它引用尚未合并到 `worker.Message/Batch` 的 `ScheduledTaskRunID`。因此 S05.1 保持 `Ready for Delivered audit`，不把该全仓 race 门禁伪称为通过。
