# S05 最终整改 blocker-only 复审（2026-07-13）

> **结论：无阻塞项，可以交付。**
>
> 本次只读复审覆盖 S05 的附件入口、动态工具协议、飞书能力代理、真实运行态和交付文档。此前的主要风险——路径未传给 App Server、目录限制与个人本机策略冲突、动态工具版本未升级、Markdown 文档未渲染、docx 读取缺失、图片被当作文件发送——均已在实现、测试或真实外部调用中得到对应闭环。

本复审不修改运行时代码、配置、数据库或既有设计；最终文档同步与本报告由交付者完成。

## 范围和新鲜证据

| 门禁 | 证据 | 判定 |
|---|---|---|
| 本机质量门禁 | `go test ./... -count=1`、`go vet ./...`、`go test -race ./internal/feishu ./internal/feishuaction ./internal/codexapp ./internal/worker -count=1` 均通过。 | 通过 |
| App Server L3 | `TestS05L3RealDynamicToolSurvivesResume` 在本机真实 `codex app-server` 下通过。 | 通过 |
| 运行态 | S05 二进制进程启动后 `/healthz=ok`，四个 enabled App receiver 均为 `connected`；`003_s05_attachments_actions.sql` 已应用。 | 通过 |
| 真实附件输入 | `attachments` 记录 image `ready=3`、file `ready=5`。 | 通过 |
| 真实能力代理 | action ledger 中 message send `succeeded=1`、file upload/send `succeeded=38`、doc create/announce `succeeded=3`、doc read `succeeded=1`。 | 通过 |
| 原生图片呈现 | 当前进程以生产 `UploadAndSend` 对 1x1 PNG 做 L4 冒烟：image upload API 与 image message 均返回成功。 | 通过 |

按用户在本轮给出的验收裁定，以上同一实现期内的历史真实成功记录可作为 L4 证据；它们的脱敏汇总已写入 [本地集成验证记录](../../story/S05-本地集成验证-2026-07-13.md)。本复审没有把 fake 或旧失败结果冒充为外部验收。

## 已核实的关键整改

| 原问题 | 当前事实 | 判定 |
|---|---|---|
| 文件/图片路径没有交给 App Server | attachment manifest 带实际可读本机路径；图片额外以 `localImage` 交给 App Server；普通文件不伪造不存在的 generic file input。 | 已解除 |
| 个人本机使用被目录限制阻断 | 仅接受可读 regular file，允许任意本机路径；仍固定当前会话目标、大小和非空约束。 | 已解除 |
| tool catalog 变更后旧 Thread 不可用 | `s05-feishu-v2` 驱动旧 Thread 在空闲边界升级，真实 resume L3 已通过。 | 已解除 |
| Markdown 文档内容未渲染 | 创建 docx 后使用 Markdown Convert，再写 descendant blocks；真实建文档 action 已成功。 | 已解除 |
| 缺少飞书文档读取能力 | `feishu.doc_read` 只接受 docx URL/ID，并以当前 App 授权读取 raw content；真实读取已成功。 | 已解除 |
| 图片以文件消息发送 | 按内容签名分流至 image 或 file API；当前进程原生图片 L4 已成功。 | 已解除 |

## 非阻塞残留项

- 运行日志仍可能出现 `im.chat.access_event.bot_p2p_chat_entered_v1` 无 handler 的非阻塞告警；它不在 S05 attachment/action 调用链上，不影响 receiver connected 或本 Story 验收。应在后续 ingress hygiene 工作中处理或显式忽略。
- 图片超过飞书 image API 的 10 MB 上限时按设计降级为文件消息；这是已声明行为，不是发送失败。
- 历史 action 密文若只由已轮换的旧本机 key 加密，将不能被新 key 解密；这不影响已经完成的外部动作，但后续轮换必须预先安排可恢复的 keyring。

## 发布决定

不存在 S05 实现或运行态 blocker。完成 Story、Story List、README、SOP、复盘和版本控制闭环后，可以将 S05 标记为 **Delivered**。
